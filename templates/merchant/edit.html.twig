{% extends 'base.html.twig' %}

{% block title %}Edit Merchant{% endblock %}

{% block body %}
<div class="flex flex-col items-center justify-between bg-[#1E1E1E] mt-20 mx-70 rounded-xl shadow-2xl min-h-120">
    <h1 class="my-4">Edit Merchant</h1>

    {{ include('merchant/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('merchant_index') }}" class="btn text-white">Back to list</a>

    {{ include('merchant/_delete_form.html.twig') }}
</div>
{% endblock %}
