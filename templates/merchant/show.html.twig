{% extends 'base.html.twig' %}

{% block title %}Merchant{% endblock %}

{% block body %}
    <h1>Merchant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ merchant.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ merchant.name }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ merchant.picture }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ merchant.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('merchant_index') }}">back to list</a>

    <a href="{{ path('merchant_edit', {slug: merchant.slug}) }}">edit</a>

    {{ include('merchant/_delete_form.html.twig') }}
{% endblock %}
