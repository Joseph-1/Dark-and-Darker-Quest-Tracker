{% extends 'base.html.twig' %}

{% block title %}Rarity{% endblock %}

{% block body %}
    <h1>Rarity</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rarity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ rarity.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_rarity_index') }}">back to list</a>

    <a href="{{ path('app_rarity_edit', {'id': rarity.id}) }}">edit</a>

    {{ include('rarity/_delete_form.html.twig') }}
{% endblock %}
